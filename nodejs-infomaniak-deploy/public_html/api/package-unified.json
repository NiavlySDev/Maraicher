{
  "name": "theronis-harvest-unified",
  "version": "1.0.0",
  "description": "Site complet Theronis Harvest avec bot Discord intégré",
  "main": "server-unified.js",
  "scripts": {
    "start": "node server-unified.js",
    "server-only": "node server.js",
    "init-db": "node -e \"require('dotenv').config(); const {initDatabase} = require('./database'); initDatabase().then(() => console.log('✅ DB initialisée')); \"",
    "import-products": "node import-products.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.0.0",
    "dotenv": "^16.3.1",
    "mysql2": "^3.6.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "express-rate-limit": "^6.8.1",
    "express-validator": "^7.0.1",
    "discord.js": "^14.13.0"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "keywords": ["site", "ecommerce", "discord", "bot", "infomaniak"],
  "author": "Theronis Harvest"
}
